[1mdiff --git a/bonus/pty/pty_exec_handling.c b/bonus/pty/pty_exec_handling.c[m
[1mindex abca173..7726dae 100644[m
[1m--- a/bonus/pty/pty_exec_handling.c[m
[1m+++ b/bonus/pty/pty_exec_handling.c[m
[36m@@ -26,14 +26,13 @@[m [mint pty_input_char_exec(terminal_buffer_t *terminal, char c)[m
         }[m
         terminal->apartenance[terminal->line_count] = -1;[m
         strcpy(terminal->lines[terminal->line_count++], terminal->current_line);[m
[31m-        terminal->current_pos = 0;[m
         memset(terminal->current_line, 0, sizeof(terminal->current_line));[m
[32m+[m[32m        terminal->current_pos = 0;[m
         terminal->actual_ligne = terminal->line_count;[m
     } else {[m
         if (get_size(terminal->current_line) < MAX_LINE_SIZE - 8) {[m
[31m-            if (!((c >= 32 && c <= 126) || (c >= 7 && c <= 13))) {[m
[32m+[m[32m            if (!((c >= 32 && c <= 126) || (c >= 7 && c <= 13)))[m
                 c = -80;[m
[31m-            }[m
             terminal->current_line[terminal->current_pos++] = c;[m
             terminal->current_line[terminal->current_pos] = '\0';[m
         } else {[m
[36m@@ -46,8 +45,8 @@[m [mint pty_input_char_exec(terminal_buffer_t *terminal, char c)[m
             }[m
             terminal->apartenance[terminal->line_count] = -1;[m
             strcpy(terminal->lines[terminal->line_count++], terminal->current_line);[m
[31m-            terminal->current_pos = 0;[m
             memset(terminal->current_line, 0, sizeof(terminal->current_line));[m
[32m+[m[32m            terminal->current_pos = 0;[m
         }[m
     }[m
     return OK;[m
[1mdiff --git a/src/main/exe_input.c b/src/main/exe_input.c[m
[1mindex cf06b2d..2e40443 100644[m
[1m--- a/src/main/exe_input.c[m
[1m+++ b/src/main/exe_input.c[m
[36m@@ -22,7 +22,7 @@[m
 #include <stddef.h>[m
 #include <stdbool.h>[m
 [m
[31m-static int get_size(hashtable_t *env, char *key)[m
[32m+[m[32mstatic int get_size_hash_val(hashtable_t *env, char *key)[m
 {[m
     char *value = NULL;[m
     int size = 0;[m
[36m@@ -59,7 +59,7 @@[m [mstatic char **materialise_env(main_data_t *data)[m
         return err_prog_n(MALLOC_ERR, ERR_INFO);[m
     env[data->env->keys_nbr] = NULL;[m
     for (int i = 0; keys[i]; i++) {[m
[31m-        if (my_malloc_c(&env[i], get_size(data->env, keys[i]) + 1) == KO[m
[32m+[m[32m        if (my_malloc_c(&env[i], get_size_hash_val(data->env, keys[i]) + 1) < 0[m
             || !my_strcat(env[i], keys[i]) || !my_strcat(env[i], "=")[m
             || !my_strcat(env[i], ht_search(data->env, keys[i])))[m
             return err_prog_n(UNDEF_ERR, ERR_INFO);[m
